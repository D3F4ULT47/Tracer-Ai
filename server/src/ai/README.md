# AI layer

The AI orchestrator resolves versioned engines through the engine registry. Each engine owns its contracts and execution behavior and may not persist roadmap state directly. Engine implementations are added only with their corresponding feature.

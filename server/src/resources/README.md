# Resource infrastructure

Resource provider adapters register through the resource adapter registry and normalize provider data into shared resource contracts. No provider adapter is implemented in the scaffold.

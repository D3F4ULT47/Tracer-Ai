# AI contracts

Versioned intent, resume, knowledge, dependency graph, planner, roadmap, resource recommendation, and optimization schemas will be introduced with their corresponding feature implementations. This scaffold contains only the shared engine result envelope; no speculative domain schema is defined before the feature contract is implemented.
